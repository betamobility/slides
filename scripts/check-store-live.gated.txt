# Published paths that are deliberately NOT reachable without a login.
#
# scripts/check-store-live.mjs walks the published tree and requires every
# path in it to answer 200 anonymously. A path listed here is exempt — so
# every line is a decision someone made, and needs the reason beside it.
#
# Keep this short. The reason the sweep is inverted is that a long list of
# exceptions is the same blind spot as no check at all.

# The root is the deck list now, and an anonymous visitor gets the Beta login
# page. That is R12 — stated as a decision rather than left as a side effect.
/

# The retired release-channel landing page. Still published so no shipped link
# 404s, but it is not an entry point any more and nothing links it (R10); its
# two plugin-install lines live in the deck list's footer. It sits behind the
# login with everything else that is not on the machine allowlist.
/index.html

# Upstream's per-app landing page. Never part of the Beta surface; the thing
# under /slides/ that machines actually fetch is /slides/agents.md, which is
# public.
/slides/
/slides/index.html

# Pages' 404 page. The plan's KTD0 listed it as public "site furniture"; that
# was thin. An unknown path is gated before it ever reaches Pages, so this
# page is never served to an anonymous visitor, and nothing fetches it by
# name. It also cost a second entry to keep public — Pages 308s an `.html`
# URL to its extensionless form, and a root-level exact entry's twin escapes
# the allowlist (measured: /404.html 308d to a gated /404). One less public
# path is the safer direction.
/404.html

# Deploy furniture for the Pages project, of no use to any caller.
/CNAME
/.nojekyll
